<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.wxcloudrun.dao.SecondHandMapper">

    <select id="getProductList" resultType="Product">
        SELECT productID,sellerAvatar,sellerNickname, productTitle,productDescription,productType,time,price,`condition`,contact,imagesJSON,delivery  
        FROM secondhand 
        WHERE productID >= #{offest} 
        ORDER BY secondhand.productID  
        LIMIT #{limit}
    </select>

    <select id="collect" resultType="boolean">
        SELECT saveProductJson
        FROM user
    </select>
    
    <update id="updateCollect" resultType="void">
        UPDATE user 
        SET productID = #{savedProductJSON} 
        WHERE UserID = #{userID}
    </update>
</mapper>
