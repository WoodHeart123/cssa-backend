<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.wxcloudrun.dao.RestaurantMapper">

    <select id="getRestaurantList" parameterType="Integer" resultType="Restaurant">
        SELECT restaurantID, name, location, avgRating, commentCount, avgPrice, ST_X(geoLocation) as longitude, ST_Y(geoLocation) AS latitude
        FROM restaurant
        WHERE avgRating &gt;= #{aboveRating} AND avgPrice &lt; #{priceLimit} AND restaurantID &gt; #{offset} LIMIT #{limit}
    </select>

</mapper>