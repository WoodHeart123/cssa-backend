<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tencent.wxcloudrun.dao.CourseMapper">

    <select id="getCourseList" parameterType="Integer" resultType="Course">
        SELECT courseID, departmentID, courseName, avgDifficulty, avgLike, credit FROM course WHERE departmentID = #{departmentID}
    </select>
</mapper>